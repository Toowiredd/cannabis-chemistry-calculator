# User-Story Register — independent code+docs review

_Generated 2026-07-26 by /userstory-audit._

## Summary

- Total: 232 · both 74 · code-only 135 · docs-only 22
- By domain: components 33 · electron-app-framework 22 · engine 70 · main-electron 9 · renderer-shell 13 · store-state 23 · tabs-ui 62

### components (33)

| id | source | prio | story | drift |
|---|---|---|---|---|
| `components-decarb-heatmap` | code_and_docs | core | As a home cook, I want a 73-130°C danger-zone bar with green/yellow/red zones, a position needle, an out-of-range warning, and a dynamic efficiency/terpene trade-off sentence, so that I can pick a decarb temperature that balances yield against terpene loss. | None observed. Validation report confirms zone geometry; below-floor warning + concentrate |
| `components-bag-calculator` | code_only | core | As a sous-vide decarb user, I want a bag-sizing surface (grind level + bag preset or custom dims + headspace gauge + double-bag recommendation + best-bag suggestion), so that I get a recipe for the right bag with safe headspace. | Component is fully wired and tested, but no docs claim names it explicitly; the broader 'b |
| `components-molecular-builder` | code_and_docs | core | As a user learning the chemistry, I want an animated THCA→THC+CO2 SVG with play/pause/scrub/speed controls, IntersectionObserver auto-start, and a reduced-motion static fallback, so that the abstract reaction becomes visual and memorable. | None observed. |
| `components-dose-radar-chart` | code_and_docs | core | As a Dose-tab user, I want a 6-axis SVG radar (THC dose / CBD dose / onset speed / duration / body load / head load) with an animated polygon, an empty-state when dose is zero, and a journal-entry comparison overlay, so that I can see the dose profile at a glance and compare against a past journal entry. | Phase1 flagged wiredToUI=false because DoseTab is out of scope. Doc evidence confirms the  |
| `components-smart-suggest-panel` | code_and_docs | core | As a Dose-tab user, I want a 4-card recipe suggestion panel (brownies/gummies/capsules/tincture) with score bars, a FLIP transition to the dose input, and a detail drawer with a 'Use This Recipe' button, so that I don't have to guess which edible format fits my potency and fat. | Phase1 flagged wiredToUI=false (no in-scope consumer); doc evidence confirms Dose tab moun |
| `components-label-generator` | code_and_docs | core | As a user finalizing a batch, I want a configurable compliance label (product name, ingredients, storage, allergens, THC per serving, classification) with a printable modal and per-save batch-number increment, so that I can affix a regulation-style label to my container. | No standalone LabelGenerator test in scope; covered indirectly by AdvancedToolsIntegration |
| `components-strain-manager` | code_only | core | As a user with a recurring strain set, I want a modal strain library (add/edit/delete strains with THCA/THC/CBDA/CBD % and optional notes) that loads from and persists to platform storage and shows per-row journal usage stats, so that I can re-pick a strain on a new batch without retyping its chemistry. | No doc claim names the component explicitly, but design language references strains as a f |
| `components-inventory-section` | code_and_docs | core | As a user tracking material on hand, I want an inline add/edit/delete inventory list with a material-kind picker (flower/concentrate/AVB), an AVB-color residual-THC estimator, a two-step destructive confirm, and toast feedback, so that the calculator can warn me when a batch exceeds my stock and I can fix it without leaving the Dashboard. | Component itself is fully implemented; the doc-described 'insufficient material gate' (3-c |
| `components-timer-widget` | code_and_docs | core | As a user running a decarb method, I want a collapsible kitchen timer that pre-fills the active method's recommended time, tracks endTime in the store, and shows a 'Done' alert on completion, so that I can walk away from the keyboard while the material decarbs and come back to a clear signal. | Phase1 marked wiredToUI=false (no in-scope consumer or test); doc claim confirms the timer |
| `components-title-bar` | code_only | core | As a desktop app user, I want a draggable frameless title bar with theme toggle, preset save/load entry, and min/max/close window controls, so that I can manage the app window and presets without leaving the keyboard. | Code uses IPC via window.App; the design doc's 'frameless + titleBarStyle hidden' is at th |
| `components-grouped-tab-nav` | code_only | supporting | As a user, I want my 9 calculator tabs organized into a 5-step workflow (Dashboard/QuickBatch/Decarb/Infusion/Dose) on a 3D carousel plus a 4-tab reference strip (Methods/Advanced/Knowledge/Journal) with a 'Next' chevron between them, so that I see the workflow progression spatially and reach reference material one tap away. | No doc claim names this directly; the design's 5-step pipeline + reference library is impl |
| `components-tab-carousel` | code_only | supporting | As a user, I want the 5 workflow tabs arranged on a 3D cylinder (N-face coverflow) with circular wrap, swipe/wheel/keyboard navigation, prefers-reduced-motion fallback, and lazy face mounting, so that the chemistry pipeline is visible spatially instead of a flat tab strip. | No doc claim names this directly; 'swipe deck' design claim refers to the older 3-face Swi |
| `components-reference-strip` | code_only | supporting | As a user, I want a flat row of 4 glass cards (Methods/Advanced/Knowledge/Journal) with icon + label + 2-bullet preview, where the active tab gets a cyan glow, so that I can pick the right reference without opening it. | No doc claim names this directly. |
| `components-transformation-canvas` | code_only | supporting | As a user, I want a full-screen ambient SVG that animates a 6x6 grid of chemical hexagons through Landing/Decarb/Infusion/Dose stages, so that the calculator surface feels alive and spatially oriented to the current step. | No doc claim names this directly; the workflow-stage accent (renderer-shell claim) is the  |
| `components-preset-actions` | code_and_docs | supporting | As a user with a tuned configuration, I want Save/Load Preset buttons with a save-name modal that captures every per-field unit and the bag override losslessly, so that I can return to a tuned configuration without re-entering every field. | None observed. PresetActions is the renderer-side wrapper over the main-electron save-pres |
| `components-toast` | code_and_docs | supporting | As a user, I want a brief on-screen confirmation when an action succeeds or fails (with 5 semantic variants — default/success/warning/danger/info — and an ARIA live region), so that I know the result without looking at the underlying state. | Phase1 evidence shows Toast is imported by InventorySection, PresetActions, and TabActions |
| `components-tooltip-icon` | code_only | supporting | As a user learning the calculator's vocabulary, I want a small (i) icon next to a field that reveals a small popover with explanatory text on hover/focus/click and is keyboard-dismissable, so that I can learn the meaning of a field without leaving the screen. | No doc claim names this; the design's 'inline validation with clear error messages' (engin |
| `components-tab-actions` | code_only | supporting | As a user on any tab, I want Export Report and Copy Summary buttons that build a tab-specific report and write it to disk or the clipboard, so that I can save a batch record or paste it into notes without leaving the tab. | Phase1 marked wiredToUI=false because consumer wiring lives in tab files out of scope; the |
| `components-startup-chooser` | code_only | supporting | As a user opening the app fresh (or returning to ambiguous state), I want a 3-intent modal chooser (make batch / resume or repeat / history or learn) with a confidence label and a recommended-intent highlight, so that I can jump to the right tab without thinking about the layout. | Phase1 status 'implemented + wired' confirms it mounts on cold start. The 'tiny chooser' d |
| `components-use-modal-a11y` | code_only | supporting | As a developer, I want one hook that handles every modal's accessibility contract (Escape-to-close, body-scroll lock, focus trap, focus-return-on-close), so that every modal in the app is keyboard-navigable and screen-reader friendly by default. | No doc claim names the hook; consumers are LabelGenerator, PresetActions, StartupChooser,  |
| `components-use-reduced-motion` | code_only | peripheral | As a developer, I want a hook that returns whether the OS prefers-reduced-motion media query is active and re-renders on change, so that I can disable animations and 3D transforms when accessibility demands it. | Phase1 flagged this as a refactor opportunity: the hook is exported and tested via the mat |
| `components-platform-bootstrap` | code_only | supporting | As a developer, I want the renderer entry point to install the web App shim automatically when running outside Electron, so that every window.App.* call site works in both desktop and PWA builds without changes. | Functionally complete; wired via side-effect import from renderer entry point (out of scop |
| `components-platform-web-shim` | code_only | supporting | As a PWA user, I want every IPC call the desktop app supports (export, copy, presets, strains, journal CRUD, open external) to work in the browser, so that the PWA build is feature-complete vs the desktop build. | No doc claim names the shim directly; the broader 'PWA install target' design claim lives  |
| `components-platform-idb-store` | code_only | supporting | As a PWA user on iOS Safari, I want my presets, strains, and journal entries to survive localStorage eviction under storage pressure, so that I don't lose my batch history when Safari reclaims quota. | No doc claim names the IDB store directly; the 'IndexedDB mirror' design claim is a relate |
| `components-swipe-deck` | code_only | peripheral | As a maintainer, I want the original 3-face workflow deck preserved as a sibling, so that any test or reference still pointing at it continues to resolve. | Phase1 marked stub_or_dead: 'Kept as a sibling for now in case any test or reference still |
| `components-lab-paste-field` | code_only | peripheral | As a user with a lab COA, I want a textarea + Apply button that parses pasted lab-report text into THCA/THC/CBDA/CBD percentages via the engine labParser, so that I don't have to hand-type the numbers from my certificate. | Phase1 marked partial: implemented in scope but no intra-scope consumer or test. The 'past |
| `components-glass-card` | code_and_docs | peripheral | As a developer, I want one <GlassCard> wrapper that applies the app's glassmorphism language (4 opacity variants — glass/glass-strong/glass-xl/opaque — with optional hover lift), so that every panel in the app has consistent translucent styling without re-typing utility classes. | Phase1 marked partial: no intra-scope consumer. The glassmorphism language is implemented  |
| `components-input-row` | code_only | peripheral | As a developer, I want a standardized label/control/error row for form fields (with ARIA alert wiring on the error message), so that every input across the app has consistent spacing and accessible error reporting. | Phase1 marked partial: no intra-scope consumer. Generic form primitive — likely consumed b |
| `components-override-badge` | code_and_docs | peripheral | As a user editing a preset, I want a small badge next to any field where I have overridden an engine-recommended value, so that I can spot my customizations without comparing every field to its default. | Phase1 marked stub_or_dead: no intra-scope consumer and no test. The pattern is rendered i |
| `components-form-validation-on-calculator-inputs` | docs_only | supporting | As a user entering calculator inputs, I want the form to validate my values inline, so that I cannot accidentally enter values that break the calculation. | Validation is implemented at the engine layer (engine/schemas.ts with zodIssuesToFieldErro |
| `components-inline-zod-validation` | docs_only | peripheral | As a user, I want inline Zod input validation with clear error messages, so that I know what is wrong as I type. | Zod schemas are implemented (engine/schemas.ts) but they are engine-side, not a components |
| `components-doneness-curve-slider` | docs_only | peripheral | As a Knowledge-tab user, I want a Doneness curve slider that tracks the value in the display unit and writes tempOverrideUnit on Apply, so that I can read the curve in my preferred unit and apply the result to the Decarb calculator without conversion mistakes. | The slider is part of KnowledgeTab (tabs-ui domain) and uses the engine doneness-simulatio |
| `components-responsive-constraints` | docs_only | peripheral | As a user with a non-standard monitor size, I want a minimum 1024×640 layout and a 1400px content max width, so that inputs fit without scrolling and text does not stretch unreadably on ultrawide monitors. | This is a shell/layout constraint, not a component-level concern. No in-scope component ow |

### electron-app-framework (22)

| id | source | prio | story | drift |
|---|---|---|---|---|
| `electron-app-framework-single-instance-lock` | code_only | supporting | As a desktop app maintainer, I want the app to acquire Electron's single-instance lock, so a second launch quits and the first window keeps focus. | no docs claim in this domain; release-pipeline wiring primitive. |
| `electron-app-framework-app-lifecycle-setup` | code_only | supporting | As a desktop app maintainer, I want a single makeAppSetup entry point that wires activate, window-all-closed, will-navigate, and platform-specific settings, so the main process behaves consistently on Windows, macOS, and Linux. | no docs claim in this domain; platform-wiring primitive. |
| `electron-app-framework-browser-window-factory` | code_only | supporting | As the main process, I want a single createWindow factory that builds a BrowserWindow from WindowProps and registers the renderer route via electron-router-dom, so every window opens with the same HTML and IPC contract. | no docs claim in this domain; window-wiring primitive. hardcodes HTML target `../renderer/ |
| `electron-app-framework-lazy-window-ipc` | code_only | supporting | As a renderer, I want to request a window over an ipcMain.handle channel, so windows are created on demand, reused on repeat calls, and torn down when closed. | no docs claim in this domain; IPC-wiring primitive. |
| `electron-app-framework-package-json-prebuild` | code_and_docs | peripheral | As a release engineer, I want a prebuild step that writes a slim package.json (no scripts/devDependencies/resources) and the pnpm trusted-deps list into the dev folder, so the packaged app does not ship dev-only fields. | doc claim 'framework-electron-vite-build' also covers clean:dev + compile:app (electron.vi |
| `electron-app-framework-semver-release-script` | code_only | peripheral | As a maintainer, I want a one-shot release script that bumps package.json, commits, tags, pushes, and opens the GitHub releases/actions pages, so I can cut a new version in a single command. | no docs claim in this domain. latent trap: checkValidations returns `true` for failure and |
| `electron-app-framework-cli-ansi-colors` | code_only | peripheral | As a release-script user, I want colored CLI output, so success/failure messages are easy to scan. | no docs claim in this domain. LIGHT_GRAY and WHITE are duplicates (`\x1b[37m`). |
| `electron-app-framework-sync-shell-exec` | code_only | peripheral | As a release tool, I want to chain shell commands sequentially via child_process.execSync, so the release pipeline can drive git without spawning an external runner. | no docs claim in this domain. `stdio: 'inherit'` only when caller passes `inherit: true`;  |
| `electron-app-framework-git-remote-extractor` | code_only | peripheral | As a release tool, I want to extract owner/repo from a git@github.com: or https://github.com/ remote URL, so I can build the releases/actions URLs without a config file. | no docs claim in this domain. hardcoded for GitHub; will not work for GitLab/Bitbucket/SSH |
| `electron-app-framework-dev-folder-resolver` | code_only | peripheral | As the prebuild step, I want to locate the dev folder under node_modules, so I can write the dist package.json next to the main entry. | no docs claim in this domain. partial — getDevFolder blindly takes the first two path segm |
| `electron-app-framework-readline-question` | code_only | peripheral | As a release tool, I want to prompt the user for a version, so the script can collect input asynchronously. | no docs claim in this domain. reads from process.stdin, so it will hang in a non-interacti |
| `electron-app-framework-semver-release-validations` | code_only | peripheral | As the release script, I want to reject invalid or non-strictly-greater versions, so a release can never produce a downgrade or duplicate tag. | no docs claim in this domain. returns `true` for failure, `undefined` for success — caller |
| `electron-app-framework-console-warning-filter` | code_only | peripheral | As the main process, I want to silence noisy third-party deprecation warnings, so the dev console is readable. | no docs claim in this domain. substring match; no way to restore the original emitter afte |
| `electron-app-framework-react-devtools-loader` | code_only | peripheral | As a developer running the app in dev, I want React DevTools auto-loaded into the BrowserWindow session, so I can inspect the React tree without manual setup. | no docs claim in this domain. hardcodes the extension path relative to cwd (not __dirname) |
| `electron-app-framework-electron-vite-build` | docs_only | peripheral | As a developer, I want an electron-vite build pipeline that prebuilds via clean:dev + compile:app + compile:packageJSON and produces main (ESM), preload, and renderer bundles, so I get a clean dist for electron-builder. | the actual build config is in electron.vite.config.ts (not in this extract). the prebuild  |
| `electron-app-framework-contextbridge-preload` | docs_only | supporting | As a security-conscious developer, I want the preload script to expose only a minimal API surface (window controls, export, clipboard, presets, platform) via contextBridge, so the attack surface stays small and no Node API leaks to the renderer. | preload code is in src/preload/index.ts (not in this extract). verify contextIsolation is  |
| `electron-app-framework-cdp-automation` | docs_only | peripheral | As an automation author, I want the Electron app to expose the Chrome DevTools Protocol so the agent-browser skill can drive tab navigation, input entry, result verification, screenshots, and console audits, so E2E coverage is automatable. | CDP is exposed by Electron by default (--remote-debugging-port); no explicit code in this  |
| `electron-app-framework-pwa-preflight-validator` | docs_only | peripheral | As a release engineer, I want a 20-check PWA pre-flight validator that runs before declaring the PWA ready, so manifest, service worker, icons, scope, start_url, apple-touch-icon, and theme-color are verified before ship. | validator script is at scripts/validate-pwa.cjs (not in this extract). CHANGELOG.md:29 con |
| `electron-app-framework-pwa-host-tailscale-funnel` | docs_only | peripheral | As a remote user, I want the PWA served via Tailscale Funnel from the static server on 127.0.0.1:8765, so I can reach the app over the Tailscale mesh when I am away from the laptop. | static server is scripts/serve-pwa.cjs (not in this extract). Tailscale Funnel is operator |
| `electron-app-framework-auto-updater` | docs_only | peripheral | As a user, I want electron-updater wired into the release script with GitHub releases, so I get in-app update notifications once that integration ships. | roadmap_planned. grep across the repo for `electron-updater`, `autoUpdater`, `updateElectr |
| `electron-app-framework-pwa-install-target` | docs_only | peripheral | As an iPad user, I want to install the app via Safari Share > Add to Home Screen, so I can use it on my tablet as a standalone app. | PWA install surface is configured in vite.web.config.ts via vite-plugin-pwa (not in this e |
| `electron-app-framework-pwa-tailscale-funnel-hosting` | docs_only | peripheral | As an iPad user on the tailnet, I want the PWA served at https://laptop.tail646a73.ts.net/ccc/ via Tailscale Funnel, so I can reach the app from outside the laptop's local network. | PWA bundle is emitted by vite.web.config.ts with `base: '/ccc/'` so every asset and the SW |

### engine (70)

| id | source | prio | story | drift |
|---|---|---|---|---|
| `engine-thc-decarbed-amount` | code_only | core | As a home cook, I want to see how much THC my chosen decarb method will actually convert from raw material, so that I can pick a method worth the time. |  |
| `engine-infused-thc` | code_only | core | As an edible maker, I want to know how much THC actually ends up in the carrier fat, so that I can plan my servings around what's really in the pot. |  |
| `engine-mg-per-serving` | code_only | core | As an edible maker, I want to know how much THC is in each piece of my batch, so that I can dose safely and label each unit. |  |
| `engine-dose-classification` | code_and_docs | core | As a user, I want the app to classify my dose (sub-microdose, micro, low, moderate, strong, very strong, extreme), so that I can decide if it's right for me without remembering the mg thresholds. |  |
| `engine-unit-conversion` | code_and_docs | core | As a user who thinks in ounces and tablespoons, I want the app to display my gram and mL values in my preferred unit without silently re-storing them, so that I keep the underlying source-of-truth intact. | doc claim 'engine-unit-conversion-helpers' tagged roadmap_planned in the audit brief, but  |
| `engine-format-number-display` | code_only | core | As a calculator user, I want every displayed number to round to 1 decimal place consistently, so that the UI never shows artifacts like 4.1000000004 mg/mL. |  |
| `engine-cost-per-dose` | code_only | core | As a cost-conscious user, I want to see what one dose of my batch costs, so that I can price my product or compare batches on equal footing. |  |
| `engine-cost-per-mg` | code_only | core | As a cost-conscious user, I want to see cost per mg of active THC, so that I can compare batches across different potencies on a normalized unit. |  |
| `engine-method-cost-comparison` | code_only | core | As a cost-conscious user, I want a side-by-side cost comparison of every decarb method on my material, so that I can pick the cheapest viable option at a glance. |  |
| `engine-thc-theoretical-max` | code_and_docs | supporting | As a chemist-user, I want the upper bound of THC my flower could produce computed via the 0.877 molecular-weight ratio, so that I know the yield ceiling before I count on decarb losses. |  |
| `engine-thc-decarb-range` | code_and_docs | supporting | As a home cook, I want to see a best/worst/likely range for my decarb yield, so that I can size the batch with realistic confidence instead of trusting a single point estimate. |  |
| `engine-cbd-theoretical-max` | code_only | supporting | As a CBD-focused consumer, I want the same theoretical-max math the THC side has for my CBDA-rich material, so that I can plan a CBD-dominant batch the same way. |  |
| `engine-cbd-decarbed-amount` | code_only | supporting | As a CBD-focused consumer, I want the expected CBD yield after decarbing, so that I can label my product with the actually-decabbed CBD value. |  |
| `engine-mg-per-ml` | code_only | supporting | As an edible maker, I want to know the potency per mL of my infusion, so that I can dose by volume (drops, spoonfuls) when I don't want to weigh the fat. |  |
| `engine-simplified-estimate` | code_and_docs | supporting | As a beginner, I want a one-input THC estimate I can trust at a glance, so that I can sanity-check a recipe without running the full 4-step workflow. |  |
| `engine-dose-display-label` | code_only | supporting | As a user, I want the canonical dose bucket (e.g. 'moderate') to display as Title-Case ('Moderate'), so the dose label reads naturally. |  |
| `engine-concentrate-theoretical-max` | code_only | supporting | As a concentrate user, I want the same theoretical-max math the flower path has for wax, shatter, distillate, hash, kief, and RSO, so that I can plan a concentrate-infused batch the same way. |  |
| `engine-concentrate-decarbed-thc` | code_only | supporting | As a concentrate user, I want the post-decarb yield for my chosen concentrate type, so that I can plan around distillate's no-decarb-needed rule. |  |
| `engine-concentrate-decarb-range` | code_only | supporting | As a concentrate user, I want a best/worst/likely range for my concentrate yield, so that I can size the batch with confidence. |  |
| `engine-bag-material-volume` | code_only | supporting | As a sous-vide decarb user, I want to know how much physical space my material takes, so that I can pick a bag that won't overflow. |  |
| `engine-bag-fill-depth` | code_only | supporting | As a sous-vide decarb user, I want to know how deep my material will sit in the bag, so that I can judge whether the seal will be tight or loose. |  |
| `engine-bag-headspace` | code_only | supporting | As a sous-vide user, I want a headspace percentage, so that I can tell whether the bag is over-stuffed, under-stuffed, or just right. |  |
| `engine-headspace-status-zones` | code_only | supporting | As a sous-vide user, I want a colored verdict on my headspace (critical/tight/optimal/loose), so that I can tell at a glance whether to add or remove material. |  |
| `engine-double-bag-recommendation` | code_only | supporting | As a safety-conscious user, I want the app to flag when I should double-bag my decarb run, so that I don't get a bag failure at 116 °C. |  |
| `engine-best-bag-selection` | code_only | supporting | As a sous-vide user, I want the app to recommend the smallest optimal bag, so that I don't waste bags or get a sloppy fill. |  |
| `engine-strain-blend-calculator` | code_only | supporting | As a user with multiple strains at different potencies, I want to know how much of each to use to hit a target potency, so that I can blend to a target instead of guessing. |  |
| `engine-doneness-simulation` | code_and_docs | supporting | As a curious user, I want to see a chart of how my decarb progresses over time, so that I can pick the time that hits peak THC before CBN takes over. |  |
| `engine-doneness-time-label` | code_only | supporting | As a user looking at the doneness chart axis, I want duration labels like '1h 30m' instead of '90', so the chart reads naturally. |  |
| `engine-lab-text-parser` | code_only | supporting | As a user with a lab COA, I want to paste the report text and have the percentages auto-populate, so that I don't have to retype the numbers from the certificate. |  |
| `engine-lab-data-detector` | code_only | supporting | As a user pasting a random text, I want the app to tell me 'no cannabinoid data found' before I submit, so that I don't have to wait for the form to fail validation. |  |
| `engine-radar-score-computation` | code_only | supporting | As a user comparing two batches, I want a 6-axis radar (THC dose, CBD dose, onset, duration, body load, head load) so I can see at a glance which batch is more sedative, faster-onset, etc. |  |
| `engine-radar-chart-geometry` | code_only | supporting | As a user, I want the radar to render as a clean 6-axis polygon with axis labels at the spokes, so that the chart is legible and visually consistent. |  |
| `engine-recipe-scaler` | code_only | supporting | As a user who made a great small batch, I want to double or halve the recipe, so that I can repeat it at a different size without re-deriving the math. |  |
| `engine-smart-recipe-suggest` | code_only | supporting | As a user who has calculated a dose, I want recipe suggestions (brownies/gummies/capsules/tincture) that match my potency, so that I can pick a format that suits what I just made. |  |
| `engine-terpene-boiling-points` | code_only | supporting | As a user, I want to look up terpene boiling points, so I can decide whether my chosen decarb temperature will preserve them. |  |
| `engine-first-timer-decarb-efficiency` | code_only | supporting | As a first-timer, I want the wizard's example decarb number to match the actual oven_sealed preset, so that the running example teaches me a real value. |  |
| `engine-strain-library` | code_only | supporting | As a user, I want to save the strains I use often (with THCA/THC/CBDA/CBD % and notes) so I don't retype their potencies every time. |  |
| `engine-avb-residual-ranges` | code_only | supporting | As a vaper reusing AVB, I want to tell the app how dark my AVB is, so that the calculator can pre-fill a reasonable residual-THC % instead of asking for an arbitrary number. |  |
| `engine-cbd-decarb-range` | code_only | peripheral | As a CBD-focused consumer, I want a low/expected/high range for my CBD yield, so that I can set realistic expectations. | implemented and tested but no UI consumer — only calculateTheoreticalMaxCbd + calculateDec |
| `engine-zod-validation-schemas` | code_and_docs | supporting | As a developer, I want the engine to expose Zod validation schemas with field-level error messages, so that input handling is type-safe at the engine boundary. |  |
| `engine-infusion-input-schema` | code_only | supporting | As a user, I want clear, friendly errors when my infusion input is invalid, so I can fix it without reading a manual. |  |
| `engine-fat-compare-input-schema` | code_only | supporting | As a user comparing carrier fats, I want the same friendly validation messages on the advanced panel that I get on the basic infusion panel. |  |
| `engine-infusion-warnings` | code_only | supporting | As a user with a very concentrated batch, I want the app to warn me when my fat volume may be too low to absorb all the decarbed THC, so that I can add more fat and avoid wasted THC. | threshold (volume < decarbedThc/20) is an engineering rule-of-thumb (audit row #39) — no p |
| `engine-zod-issues-to-field-errors` | code_only | supporting | As a developer, I want one helper to map Zod's nested issues to {field: message}, so my form components stay simple. |  |
| `engine-decarb-input-warning` | code_only | peripheral | As a user with a very high-potency input, I want a 'double-check your lab' warning when my THCA+THC or CBDA+CBD exceeds 40%, so that I can sanity-check before trusting the math. | implemented at schemas.ts:139 but no tab imports it; the older duplicate getDecarbWarnings |
| `engine-decarb-method-presets` | code_only | supporting | As a user, I want a menu of named decarb methods (4 sous-vide + 2 oven) with temperature, time range, and efficiency range, so that I can pick the one I have equipment for. | efficiency ranges are engineering estimates (audit row #19) — bracketed ranges not directl |
| `engine-infusion-fat-presets` | code_and_docs | supporting | As an edible maker, I want a menu of named carrier fats (ghee, coconut, MCT, custom) with extraction efficiency, so that I can pick the right fat for my recipe. | efficiencies and simplified multipliers are engineering estimates (audit row #18) — MCT >  |
| `engine-edible-format-presets` | code_only | supporting | As an edible maker, I want a list of common formats (brownie 9x13 / 8x8, gummy 80 / 160, capsule 00, custom) with typical serving counts, so that I can size my batch without guessing. | serving counts are recipe/packaging conventions (audit row #22) — not from a peer-reviewed |
| `engine-wizard-card-data` | code_only | supporting | As a first-timer running the wizard, I want each method/fat/format card to show a one-line 'when to use this' note, so that I can pick without reading a manual. | selectionTip is a documented carry-forward gap (only humanNote renders today); falls back  |
| `engine-build-export-report` | code_only | supporting | As a user, I want a copy-and-paste text report of my full batch (with a sibling JSON for machine consumption) so I can save or share it. |  |
| `engine-build-tab-copy-text` | code_only | supporting | As a user, I want to copy just the current tab's summary, so I can share a single section without exporting the whole report. |  |
| `engine-avb-theoretical-max` | code_only | peripheral | As a vaper reusing AVB, I want the engine to handle the no-decarb-needed case for AVB material, so that my numbers match what the vaporizer already did. | both calculateAvbTheoreticalMax and calculateAvbTheoreticalMaxFromColor are only exercised |
| `engine-thc-degradation-kinetics` | code_only | peripheral | As a long-term storer, I want to know how much THC my product will lose over time at my storage temperature, so that I can pick a sensible shelf life. | all public functions (calculateDegradation, calculateRemainingThcPercent, calculateCbnForm |
| `engine-bag-volume` | code_only | peripheral | As a user sizing a custom bag, I want to know the interior volume, so I can plug it into the fill-depth and headspace math. | BAG_PRESETS in models.ts pre-compute volumeCm3 at the preset level; the runtime BagCalcula |
| `engine-lab-text-strict-parser` | code_only | peripheral | As a developer using the engine in batch mode, I want a parser that fails loud on bad input, so that I can catch garbage input programmatically. | implemented and tested but no UI surface calls it; the LabPasteField uses the lenient pars |
| `engine-recipe-save-load` | code_only | peripheral | As a user, I want to save my current inputs to a named recipe and reload it later, so that I can repeat a batch without re-typing everything. | saveRecipe / loadRecipe are implemented and tested but no tab or component currently calls |
| `engine-recipe-format-name-lookup` | code_only | peripheral | As a user, I want a friendly name for a format id (e.g. 'brownie_9x13' → 'Brownie (9×13 pan, 12-24 servings)'), so the recipe card title reads naturally. | implemented and tested but no UI imports it; the recipe-scorer carries the full name on ea |
| `engine-reverse-full-workflow` | code_only | supporting | As a user who knows the dose I want, I want the app to back-solve how much material I need, so that I don't have to invert the 4-step math in my head. |  |
| `engine-reverse-decarb` | code_only | peripheral | As a user who only needs to solve the decarb step, I want a focused reverse-decarb helper, so that I can size material without dragging in the infusion math. | implemented and tested but the UI uses reverseFullWorkflow end-to-end instead; available f |
| `engine-wizard-options-aliases` | code_only | peripheral | As a downstream UI task, I want imports named METHOD_OPTIONS / FAT_OPTIONS / FORMAT_OPTIONS, so my brief's symbol path resolves cleanly. | file uses @ts-nocheck because the source-of-truth typecheck is governed by wizardPresets.t |
| `engine-wizard-recipe-lookups` | code_only | peripheral | As the wizard, I want to look up a card by id, so that picking a recipe pre-fills the next step with curated defaults. | all four functions are implemented and tested but no UI imports them; FirstTimerGuide uses |
| `engine-grind-level-presets` | code_only | peripheral | As a sous-vide user, I want a pickable grind level (coarse / medium / fine), so the bag-size estimate matches my actual grind. | cm3PerGram values (coarse 6.0, medium 3.5, fine 2.2) are engineering heuristics for sous-v |
| `engine-bag-presets` | code_only | peripheral | As a sous-vide user, I want a menu of common bag sizes (3 zip + 2 vacuum), so the app can recommend the right one. | 5 entries — quart (61.4 cm³), gallon (195.3), 2-gallon (435.4), small_vac (57.9), large_va |
| `engine-strain-model` | code_only | peripheral | As a developer, I want a stable Strain type that both the engine and the strain picker agree on, so the model and view never drift. |  |
| `engine-evaluate-startup-routing` | code_only | peripheral | As a returning user, I want the app to land me where I was last working, so that I can pick up where I left off without re-finding my tab. | imported by src/screens/main.tsx:67; this is a utility, not pure engine — included in scop |
| `engine-destination-for-startup-intent` | code_only | peripheral | As a user, I want a chooser where each option takes me somewhere useful, so that picking 'make a batch' lands me in the QuickBatch flow. | imported by src/screens/main.tsx:66; utility, included in engine scope because it bridges  |
| `engine-1000-plus-vitest-tests` | docs_only | peripheral | As a developer, I want 1,000+ deterministic engine unit tests covering happy path, boundaries, errors, reversibility, and migrations, so that the calculation engine is regression-safe. | doc claim; test count exists in DESIGN.md but no specific test-suite capability in the eng |
| `engine-constant-audit-discipline` | docs_only | peripheral | As a reviewer, I want every numeric/qualitative engine constant traceable to a peer-reviewed source, an explicit TODO flag, or a NIST/SI factor, so that the chemistry is defensible. | doc claim about a review process; the underlying audit rows are referenced in every engine |
| `engine-pure-typescript-no-ui-deps` | docs_only | peripheral | As a developer, I want the calculation engine to be pure TypeScript with no React/Electron imports, so that the engine is portable, testable, and isolated from UI changes. | doc claim; verifiable by greping engine/ for 'from react' / 'from electron' — no specific  |
| `engine-display-mapper-engine-delegation-pattern` | docs_only | peripheral | As a developer, I want every displayed value in a tab to be computed by an engine function, so that display logic is not re-implemented inline in tab components. | doc claim about a coding discipline; supported by code+docs reconciliation (format-number- |

### main-electron (9)

| id | source | prio | story | drift |
|---|---|---|---|---|
| `main-electron-frameless-window-with-custom-chrome` | code_and_docs | core | As a desktop app user, I want a frameless Electron window with a custom titlebar that exposes min/max/close controls, so that the UI extends edge-to-edge without a visual OS title bar break. | Docs say 'frameless: true' but code uses `frame: false` (semantically equivalent in Electr |
| `main-electron-export-copy-preset-ipc` | code_and_docs | core | As a user, I want the main process to handle export-report (native save dialog with .txt + .json), copy-to-clipboard, save-preset (hash-deduped JSON in %APPDATA%/presets/), and load-preset-dialog over IPC, so that the renderer can persist and share results through native OS surfaces. |  |
| `main-electron-journal-disk-persistence` | code_and_docs | core | As a journal user, I want journal entries persisted to disk via the Electron IPC bridge (not the Zustand store), so that my history survives store resets and is portable across reinstalls. |  |
| `main-electron-strain-library-persistence` | code_only | core | As a user with custom strains, I want my strain library persisted to disk via the main process IPC, so that my custom strains survive app restarts and reinstalls. | No docs claim for strain library disk persistence found in the main-electron domain docs ( |
| `main-electron-open-external-link` | code_only | core | As a user, I want to open an external link (web page or mailto) from inside the app via the main process, so that I can follow references without leaving the workflow. | No docs claim for the open-external IPC handler found in the main-electron domain docs. Pr |
| `main-electron-platform-info` | code_only | core | As the renderer, I want access to the current OS platform string via the App bridge, so that the UI can adapt (e.g. shortcut hints, platform-specific affordances) to feel native on each OS. | No docs claim for the platform-info exposure found in the main-electron domain docs. Read- |
| `main-electron-windows-nsis-portable-zip` | code_and_docs | peripheral | As a Windows user, I want packaged distribution in three forms (NSIS installer with shortcuts/uninstaller, single-file portable .exe, and zip archive), so that I can install, carry on USB, or extract manually. | The electron-builder.ts packaging config was not listed as a code capability in the phase1 |
| `main-electron-macos-dmg` | code_and_docs | peripheral | As a macOS user, I want a .dmg installer target configured (with testing still needed on Apple Silicon), so that I can install the app on macOS once that verification lands. | The electron-builder.ts packaging config was not listed as a code capability in the phase1 |
| `main-electron-linux-appimage-deb-rpm` | code_and_docs | peripheral | As a Linux user, I want AppImage, .deb, and .rpm targets configured (with testing still needed on Ubuntu and Fedora), so that I can install the app on Linux once verified. | The electron-builder.ts packaging config was not listed as a code capability in the phase1 |

### renderer-shell (13)

| id | source | prio | story | drift |
|---|---|---|---|---|
| `renderer-shell-main-screen-chrome` | code_and_docs | core | As a user, I want a stable chrome (TitleBar, brand header, glass-panel main area, footer disclaimer) around every tab, so that title, brand, and disclaimer are always present regardless of which tab is active. | Docs claim 'top nav with all main tab buttons' is false: the top header (main.tsx:253-281) |
| `renderer-shell-first-paint-overlay` | code_and_docs | core | As a user on cold start, I want a branded loading overlay for ~800ms that fades out over ~350ms, so that the shell doesn't flash unstyled content before the carousel mounts. |  |
| `renderer-shell-theme-application` | code_and_docs | core | As a user switching themes, I want the .dark class toggled on the root <html> based on the appStore theme value, so that all CSS variables re-bind to the correct light/dark palette. |  |
| `renderer-shell-workflow-stage-accent` | code_only | core | As a user moving through the chemistry pipeline, I want the chrome accent to change with my current stage, so that the visual flow Decarb → Infusion → Dose is reinforced. | No docs claim directly calls out body[data-workflow-stage]; the stage-accent CSS is docume |
| `renderer-shell-electron-html-shell` | code_and_docs | supporting | As a renderer, I want a secure HTML host with a known mount point and a strict CSP that allows data: img-src, so that the React entry can bootstrap predictably under Electron's CSP and the SVG noise background is not blocked. |  |
| `renderer-shell-react-app-bootstrap` | code_only | supporting | As a user launching the app, I want the renderer to mount React 18 root into <app> under StrictMode and load fonts + the platform shim + the route tree, so that first paint resolves to the styled shell. |  |
| `renderer-shell-single-route-routing` | code_only | supporting | As the Electron main process, I want a single '/' route mounted via lib/electron-router-dom's Router that renders MainScreen, so that MainScreen is the only top-level view and Electron's BrowserWindow routing is respected. |  |
| `renderer-shell-startup-chooser-heuristic-manual-escape` | code_and_docs | supporting | As a returning user, I want the startup heuristic to auto-route me to my last successful path when confidence is high, show a 3-intent chooser when it's not, and always expose a manual 'Choose Start' escape hatch in the nav, so that I am never trapped in a wrong heuristic and never re-choose my intent on every launch. |  |
| `renderer-shell-first-time-wizard-boot` | code_and_docs | supporting | As a first-time user, I want the app to drop me into the multi-select wizard over the Quick Batch tab on first launch, so that I learn the workflow before I start tweaking inputs, and never re-prompted if I already opted out. |  |
| `renderer-shell-pwa-install-surface` | code_and_docs | peripheral | As an iPad user, I want a complete static icon set (favicon, apple-touch-icon, PWA 64/192/512 + maskable) so that the home-screen launcher, splash, and tab favicon all resolve without 404s when the renderer is served as a PWA. | Docs claim bundles the window.App web-shim swap into the same story, but web-shim lives un |
| `renderer-shell-cn-classname-helper` | code_only | peripheral | As a component author, I want a Tailwind-aware cn() helper that composes clsx + tailwind-merge, so that conflicting utilities resolve to the last-specified one and conditional classes stay readable. |  |
| `renderer-shell-alert-ui-primitive` | code_only | peripheral | As a developer, I want a styled shadcn/ui Alert primitive (Alert/AlertTitle/AlertDescription with default + destructive variants) installed, so that feature work can drop in semantic banners without rebuilding the variant system. | Status is stub_or_dead: the primitive is fully implemented and uses cn(), but a ripgrep of |
| `renderer-shell-vertical-stacked-layout-drift` | drift_confirmed | peripheral | As a validation report reader, I would have expected all tab content sections to render simultaneously in a vertically stacked layout, so that I can scroll through everything without navigating hidden tabs. | DRIFT: MainScreen (main.tsx:324-409) renders a single GroupedTabNav with a 3D TabCarousel  |

### store-state (23)

| id | source | prio | story | drift |
|---|---|---|---|---|
| `store-state-persisted-units-slice` | code_and_docs | core | As a home-extraction hobbyist, I want my preferred unit system (C/F, g/oz, mL/tsp/tbsp/cup, cm/in) to be persisted under the ccc-app-state key, so that my unit choice survives app restart. | Design doc says 'ten slices' but partialize has 11 top-level keys (the 11th being wizard w |
| `store-state-persisted-decarb-slice` | code_and_docs | core | As a home-extraction hobbyist, I want every Decarb tab field (weight, percentages, preset, overrides, bag config, strainId, materialMode) to be persisted, so that I can close the app and return to my decarb config exactly as I left it. | none significant |
| `store-state-persisted-infusion-slice` | code_and_docs | core | As a home-extraction hobbyist, I want every Infusion tab field (decarbedThc, volume, volumeUnit, fatId, customEfficiency) to be persisted, so that my infusion setup survives restart. | none significant |
| `store-state-persisted-dose-slice` | code_and_docs | core | As a home-extraction hobbyist, I want every Dose tab field (totalThc, servings, formatId, reverseMode, desiredMgPerServing) to be persisted, so that I can return to my dose calculation exactly. | none significant |
| `store-state-persisted-advanced-tools-slice` | code_and_docs | core | As a home-extraction hobbyist, I want every Advanced Tools sub-form (concentrate, blending, cost) and the active sub-tab to be persisted, so that I can switch to a different tab and return to the same Advanced sub-tool with my inputs intact. | none significant |
| `store-state-persisted-startup-routing-slice` | code_and_docs | core | As a startup system, I want launchCount, chooserShownCount, lastIntent, and successfulPath telemetry persisted, so that the startup heuristic can distinguish a confident-return user from a first-timer on next launch. | none significant |
| `store-state-persisted-theme-slice` | code_and_docs | core | As a user, I want my dark/light theme choice persisted, so that the app reopens in the same visual mode I left it in. | none significant |
| `store-state-persisted-label-slice` | code_and_docs | core | As a home-extraction hobbyist making edibles, I want my Label form (productName, ingredients, storage, batchNumber, allergens, productionDate) and the increment-batch-number action to be persisted, so that successive labels for the same product get distinct batch numbers across restarts. | none significant |
| `store-state-persisted-inventory-slice` | code_and_docs | core | As a home-extraction hobbyist, I want my inventory (lowStockThreshold + items array with kind: flower/concentrate/avb) to be persisted, so that my on-hand material and stock thresholds survive restart. | none significant |
| `store-state-persisted-first-run-dismissed` | code_and_docs | core | As a user, I want the firstRunDismissed flag to be persisted, so that the app stops showing the first-run hint after I've seen it once. | none significant |
| `store-state-persist-versioned-migrations` | code_and_docs | core | As a long-time user upgrading through multiple app versions, I want the store to apply a chained v0→v1→v2→v3→v4→v7 persist migration (wizard slice, journal source, inventory kind, journal materialWeightUnit, firstTimerOpen alias collapse, dismiss merge, per-tab unit normalization) so my history and inventory survive upgrades without manual repair. | Design doc says 'v1→v7' but the code actually chains v0→v1, v1→v2, v2→v3, v3→v4, and v4→v7 |
| `store-state-persist-key-rename` | code_and_docs | core | As a returning user upgrading from the old key, I want the store to rehydrate from the legacy 'cannabis-chem-units' key on first launch and write the migrated envelope to the new 'ccc-app-state' key, so that my data survives the rename with no manual export/import. | The key rename itself is documented in the partialize comment but not called out as a sepa |
| `store-state-per-field-unit-tracking` | code_and_docs | core | As a user toggling units, I want each input field (decarb weight, infusion volume, decarb temperature override, bag width/length override) to remember the unit I typed it in, so that toggling the display unit does not silently misread or precision-drift my value. | The audit-briefs claim tags the migration guards as 'roadmap_planned' but the code already |
| `store-state-cross-tab-carry-forward` | code_and_docs | core | As a home-extraction hobbyist moving between tabs in a session, I want the decarb expected-mg value to auto-feed the Infusion tab, and the Infusion total-mg to auto-feed the Dose tab, so that I don't retype the same number across the 3-step pipeline. | Cross-tab carry-forward is IN-SESSION ONLY. lastDecarbedThc and lastInfusedThc are NOT in  |
| `store-state-load-preset-snapshot` | code_and_docs | core | As a home-extraction hobbyist, I want to bulk-replace units + decarb + infusion + dose from a saved preset/recipe snapshot, with invalid unit literals coerced to safe defaults and malformed snapshots ignored, so that loading a preset is lossless and never crashes the tabs. | none significant |
| `store-state-active-tab-not-persisted` | code_only | core | As a returning user, I want the app to open on a default tab (decarb) rather than replaying the tab I last visited, so that accidental visits to a tab don't reopen me on the wrong surface on next launch. | By design — not currently claimed in the docs as a feature; partialize comment 1703-1707 e |
| `store-state-strain-library-runtime-only` | code_only | core | As a home-extraction hobbyist, I want my strain library (add/update/delete/list) bulk-replaced in the store, so the StrainManager UI sees my custom strains immediately — with the source-of-truth living on disk (loaded on demand), not in the Zustand envelope. | no direct store-state docs claim; the strain lib contract is documented in the engine extr |
| `store-state-journal-disk-persistence` | code_and_docs | core | As a home-extraction hobbyist, I want journal entries (with `source` and `materialWeightUnit` provenance) appended and deleted in the store, with the source-of-truth persisted to disk (and mirrored to IndexedDB on the PWA), so that my batch history survives store resets and is portable across reinstalls. | Design doc only documents the on-disk truth; the IDB mirror claim is in CHANGELOG. Both ar |
| `store-state-wizard-selections-persisted-runtime-reset` | code_and_docs | supporting | As a first-time user configuring my kit, I want my wizard selections (equipment, decarbMethodIds, fatIds, formatIds, grams, thcaPct, servings, fatVolume) and the dismissed flag to be persisted across launches, but the modal's open/closed state and step index to reset to false/0 on every reload, so that the wizard never re-opens itself but my kit choices stick. | Design doc lumps wizard in with the '10 slices' but does not call out the partial persist  |
| `store-state-wizard-fat-volume-field` | code_only | supporting | As a first-time user, I want the wizard to remember my fat volume alongside grams, THCA%, and servings, so that the saved batch reflects the numbers I actually entered in the kit configurator. | No docs claim; the comment at appStore.ts:221 explicitly states fatVolume was added in the |
| `store-state-timer-runtime-only` | code_only | supporting | As a home-extraction hobbyist on the Decarb tab, I want a kitchen-timer (active, endTime, totalSeconds, methodName) that I can start, stop, and reset within a session, so that I get a 'done' alert when my decarb method finishes — with the timer state resetting on reload so I never get a stale alert after a restart. | No direct docs claim in the store-state docs; the timer widget is documented in the compon |
| `store-state-onboarding-dismiss-action` | code_and_docs | supporting | As a user, I want a single dismissOnboarding action that sets firstRunDismissed=true, closes the modal in the current session, and stamps wizard.dismissed=true (persistent opt-out), so that the calculator stops re-prompting me on every launch. | none significant |
| `store-state-legacy-dismiss-shims` | code_only | peripheral | As a legacy caller (FirstTimerGuide.tsx and the wizard test fixture), I want dismissFirstRun and dismissWizard — the pre-Cluster-C aliases for dismissOnboarding — to keep working, so that pre-refactor code paths and test fixtures don't break on the rename. New code MUST use dismissOnboarding. | No docs claim; marked @deprecated in code. Removal pending a future migration. |

### tabs-ui (62)

| id | source | prio | story | drift |
|---|---|---|---|---|
| `tabs-ui-dashboard-overview` | code_and_docs | supporting | As a returning user, I want a Dashboard with summary stats, a low-stock alert, and a togglable secondary-stats section, so that I can understand my recent activity without navigating further. |  |
| `tabs-ui-dashboard-low-stock-alert` | code_only | supporting | As a user, I want a prominent warning when my material on hand is low, so that I can restock before my next batch. |  |
| `tabs-ui-dashboard-inventory-management` | code_and_docs | supporting | As a user, I want to log material purchases and usage inline on the Dashboard, so that the rest of the app can read inventory state without a separate tab. |  |
| `tabs-ui-dashboard-charts` | code_only | supporting | As a user, I want to see how my batches are distributed over time and how my potency has trended, so that I can spot patterns in my results. |  |
| `tabs-ui-decarb-calculator` | code_and_docs | core | As a Decarb user, I want to enter material weight, cannabinoid percentages, and a decarb method, so that I can see my expected decarboxylated THC output. |  |
| `tabs-ui-decarb-strain-and-lab-input` | code_only | core | As a user with a saved strain library or a lab report, I want to pick a strain or paste a CoA so that the calculator fills THCA/THC/CBDA/CBD automatically. |  |
| `tabs-ui-decarb-method-and-overrides` | code_and_docs | core | As a user, I want to choose a decarb method and optionally override its temperature, time, and efficiency, so that I can match a custom recipe. |  |
| `tabs-ui-decarb-avb-mode` | code_only | core | As a user working with already-vaped bud, I want to pick a color that matches my AVB, so that the calculator uses a typical residual-THC value I can fine-tune. |  |
| `tabs-ui-decarb-advanced-cbd` | code_only | core | As a user with CBD-rich material, I want to expand the advanced CBD fields, so that I can track the full cannabinoid profile. |  |
| `tabs-ui-decarb-inventory-warnings` | code_and_docs | core | As a user, I want to know if I do not have enough material for the batch I am planning, so that I can restock or pick a smaller batch. |  |
| `tabs-ui-decarb-reset` | code_only | core | As a user, I want to clear my current decarb entries, so that I can start over without manually clearing each field. |  |
| `tabs-ui-infusion-calculator` | code_and_docs | core | As a user, I want to enter decarbed THC, my fat, and the fat volume, so that I can see the total infused THC and concentration. |  |
| `tabs-ui-infusion-custom-efficiency` | code_only | core | As a user with my own carrier fat, I want to type my own extraction efficiency, so that the calculator matches my recipe. |  |
| `tabs-ui-infusion-volume-unit-toggle` | code_and_docs | core | As a user, I want to read the fat volume in my preferred unit, so that I do not have to convert it in my head. |  |
| `tabs-ui-infusion-show-formula` | code_only | core | As a user, I want to see the math behind the numbers, so that I understand how the calculator works. |  |
| `tabs-ui-infusion-reset` | code_only | core | As a user, I want to clear the infusion calculator, so that I can start over. |  |
| `tabs-ui-dose-forward-calculator` | code_and_docs | core | As a user, I want to enter the total infused THC and the number of servings, so that I can see mg/serving and a safety classification. |  |
| `tabs-ui-dose-reverse-calculator` | code_and_docs | core | As a user, I want to specify the dose I want per serving, so that the calculator tells me how much starting material I need. |  |
| `tabs-ui-dose-classification-scale` | code_and_docs | core | As a user, I want to see where my dose falls on a sub-microdose-to-extreme scale, so that I can judge safety at a glance. | Docs use 4-tier (micro/standard/strong/heavy); code uses 7-band scale (sub-microdose throu |
| `tabs-ui-dose-edible-format` | code_only | core | As a user, I want to pick a common edible format, so that the servings field pre-fills with a typical count. |  |
| `tabs-ui-dose-scale-batch` | code_only | core | As a user, I want to rescale my entire recipe, so that I can make a half or double batch without recomputing each step. |  |
| `tabs-ui-dose-label-generator` | code_only | core | As a user, I want a printable label for my batch, so that I can attach it to the storage container. |  |
| `tabs-ui-dose-show-formula` | code_only | core | As a user, I want to see the formula behind the dose math, so that I can verify it for myself. |  |
| `tabs-ui-dose-reset` | code_only | core | As a user, I want to clear the dose calculator, so that I can start a fresh calculation. |  |
| `tabs-ui-quickbatch-5-step-wizard` | code_and_docs | core | As a first-time or guided user, I want a 5-step Quick Batch wizard (Material → Method → Fat & Volume → Servings & Dose → Label & Save) with destination-aware Next labels, so that I can complete a batch without jumping tabs. |  |
| `tabs-ui-quickbatch-load-last-batch` | code_only | core | As a repeat user, I want to start from my last saved batch, so that I can tweak instead of re-entering every field. |  |
| `tabs-ui-quickbatch-label-and-save` | code_and_docs | core | As a guided user, I want the Save Batch to Journal action to persist my generated batch with source=quickbatch, so that I can revisit and repeat it later. |  |
| `tabs-ui-quickbatch-reset` | code_only | core | As a user, I want to wipe the whole calculator, so that I can start a fresh batch. |  |
| `tabs-ui-journal-load-entries` | code_and_docs | core | As a user, I want to see my saved batches when I open the Journal tab, so that my history persists across sessions. |  |
| `tabs-ui-journal-new-entry-form` | code_only | core | As a user, I want a complete form to enter a new batch, so that I can capture every field the journal needs. |  |
| `tabs-ui-journal-log-from-calculator` | code_and_docs | core | As a user on the Dose tab, I want to send my calculation to the journal, so that the saved entry preserves the same totalThc, mgPerServing, and servingUnit values shown in the tab. |  |
| `tabs-ui-journal-save-entry` | code_only | core | As a user, I want to save a journal entry, so that it survives across sessions. |  |
| `tabs-ui-journal-delete-entry` | code_and_docs | core | As a user, I want to delete a saved batch, so that I can remove entries I no longer care about. |  |
| `tabs-ui-journal-search-and-filter` | code_only | core | As a user with many saved batches, I want to search and filter the list, so that I can find a specific entry quickly. |  |
| `tabs-ui-journal-timer` | code_only | core | As a user, I want a kitchen timer on the Journal tab, so that I can time my infusion or decarb step without leaving the app. |  |
| `tabs-ui-methods-comparison` | code_and_docs | supporting | As a user, I want to see all decarb methods compared on a single screen, so that I can pick the one that maximizes potency or terpene retention. |  |
| `tabs-ui-methods-use-this` | code_and_docs | supporting | As a user, I want to apply a method I compared to the main Decarb calculator, so that the decarb step uses my pick. |  |
| `tabs-ui-methods-avb-callout` | code_only | supporting | As a user in AVB mode, I want to be told why the Use This button is gone, so that I know where to go next. |  |
| `tabs-ui-methods-reset` | code_only | supporting | As a user, I want to clear the comparison inputs, so that I can start a fresh comparison. |  |
| `tabs-ui-advanced-fat-comparison` | code_and_docs | supporting | As a user exploring fat options, I want a Fat Comparison sub-tab that lets me compare per-fat efficiency, toggle volume units, and see the comparison output, so that I can pick the best fat for my recipe. |  |
| `tabs-ui-advanced-concentrate-calculator` | code_and_docs | supporting | As a user with concentrates, I want to estimate their decarboxylated output and apply the result to the Decarb tab, so that I can plan a batch in one click. |  |
| `tabs-ui-advanced-strain-blending` | code_only | supporting | As a user with multiple strains, I want to find the blend that hits my target potency, so that I can mix them precisely. |  |
| `tabs-ui-advanced-cost-analysis` | code_only | supporting | As a cost-conscious user, I want to see how each decarb method affects the price per dose, so that I can pick the most economical approach. |  |
| `tabs-ui-advanced-sub-tab-nav` | code_and_docs | supporting | As a user, I want to switch between the four advanced tools, so that I can use the right one for my current question. |  |
| `tabs-ui-advanced-reset` | code_only | supporting | As a user, I want to clear the advanced tool inputs, so that I can compare fresh values. |  |
| `tabs-ui-knowledge-explainer-sections` | code_and_docs | supporting | As a curious user, I want to read the science behind the calculator, so that I understand why the numbers are what they are. |  |
| `tabs-ui-knowledge-doneness-curve` | code_only | supporting | As a user, I want to drag the temperature and time sliders, so that I can see the kinetic tradeoffs in real time. |  |
| `tabs-ui-knowledge-apply-curve-to-decarb` | code_and_docs | supporting | As a user who just explored a curve, I want to apply it to the real Decarb tab, so that I do not retype the values. |  |
| `tabs-ui-knowledge-citations` | code_only | supporting | As a user, I want to follow citation links, so that I can read the underlying research. |  |
| `tabs-ui-knowledge-terpene-table` | code_only | supporting | As a user, I want to look up terpene boiling points, so that I can understand why a given temperature affects flavor. |  |
| `tabs-ui-first-timer-guide-wizard` | code_and_docs | peripheral | As a beginner with no equipment or material, I want an 8-step wizard with live math previews, so that I can walk from 'I have nothing' to a saved journal entry. |  |
| `tabs-ui-wizard-step-jump-and-nav` | code_only | peripheral | As a user in the wizard, I want to jump back to a previous step, so that I can correct a choice without starting over. |  |
| `tabs-ui-wizard-visual-pickers` | code_only | peripheral | As a first-timer, I want to see what each grind / bag / packing option looks like, so that I can make a confident choice. |  |
| `tabs-ui-wizard-use-decarb-shortcut` | code_only | peripheral | As a user who already entered material data on the Decarb tab, I want to import it into the wizard, so that I do not retype it. |  |
| `tabs-ui-wizard-review-matrix` | code_only | peripheral | As a first-timer, I want to see all my method / fat / format combinations side by side, so that I can pick the one that fits my needs. |  |
| `tabs-ui-wizard-save-or-open` | code_only | peripheral | As a first-timer at the end of the wizard, I want to either save my batch or hand off to the full calculator, so that I can continue without re-entering data. |  |
| `tabs-ui-realtime-recalc-debounce` | docs_only | core | As a typing user, I want input changes to trigger recalculation only after 300 ms of inactivity, so that intermediate results do not flicker and CPU stays cool during rapid typing. | Docs claim a 300ms debounce. The tabs-ui code extract does not surface an explicit debounc |
| `tabs-ui-cross-tab-unit-toggles` | docs_only | core | As a user, I want unit toggles for temperature, bag width/length in Decarb Advanced Settings, the wizard's fat picker, and the First-Timer Guide, so that I can use my preferred units throughout the app without losing the underlying value. | The Infusion tab has an explicit volume unit toggle (InfusionTab.tsx:301, 518). The audit  |
| `tabs-ui-result-panel-aria-live` | docs_only | core | As a user relying on assistive technology, I want the result panel to be an aria-live region that announces updates, so that I hear the new dose result and classification when the inputs change. | Doc claim strength is 'ambiguous' — no quoted file:line for the actual aria-live attribute |
| `tabs-ui-reduced-motion-respect` | docs_only | supporting | As a user with motion sensitivity, I want the unit-toggle transition and the result-panel result-bloom animation to respect useReducedMotion, so that motion-triggered transitions do not affect me. | Doc claim strength is 'ambiguous'. The components extract contains a useReducedMotion hook |
| `tabs-ui-vertical-stacked-layout-drift` | docs_only | peripheral | As a stale doc reader, I want all sections visible at once in a vertically stacked layout, so that I can scroll through everything without navigating hidden tabs. | REAL DRIFT. The validation_report.md claim that all 7 tab sections are simultaneously rend |
| `tabs-ui-6-tab-scientific-calculator-drift` | docs_only | peripheral | As a stakeholder reading product research, I want a 6-tab scientific calculator with dark mode and glassmorphism, so that I have a professional desktop tool for cannabis chemistry work. | REAL DRIFT. Product research calls for a 6-tab scientific calculator; the actual product i |

